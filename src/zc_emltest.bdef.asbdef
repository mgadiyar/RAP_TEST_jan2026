projection;
strict ( 2 );
use draft;

define behavior for ZC_EMLTEST //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Resume;
  use action Activate;
  use action Discard;
  use action Prepare;

  use association _result { create; with draft; }
}

define behavior for ZC_STURESULTun //alias <alias_name>
{
  use update;
  use delete;

  use association _Student { with draft; }
}